# Copy to .env and fill your keys (never commit real secrets)
# Google Generative Language API key (Gemini / Embeddings)
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY

# Optional overrides (defaults shown)
CHAT_MODEL=gemini-1.5-flash
EMBED_MODEL=models/text-embedding-004

# Dev only: override Python binary discovery for local CLI fallbacks
# PYTHON_BIN=/usr/bin/python3

# Optional: explicitly point Node to an HTTP base for the Python /api/rag
# RAG_HTTP_ENDPOINT=https://your-deployment-url
# RAG_BASE_URL=http://127.0.0.1:3000